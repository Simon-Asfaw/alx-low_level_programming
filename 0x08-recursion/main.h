#include <unistd.h>

/**
 * _puts_recursion - writes a character recursivly
 * @s: char
 * Return: On success 1
 * On error, -1 is returned, and errno is set appropriately.
 */

void _puts_recursion(char *s);
