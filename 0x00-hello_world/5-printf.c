#include <stdio.h>

/**
 * main - the function that starts
 * Description: what we gonna do is use printf
 * Return: zero 0 nada
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
