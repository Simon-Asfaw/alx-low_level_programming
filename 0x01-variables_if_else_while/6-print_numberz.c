#include <stdio.h>

/**
 * main - wa
 * Description: na
 * Return: di
 */

int main(void)
{
	int a;

	for (a = 0; a < 10 ; a++)
	{
		char z = a + '0';
		putchar(z);
	}
	putchar('\n');
	return (0);
}

